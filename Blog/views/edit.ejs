<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Edit</title>
    <script src="https://cdn.jsdelivr.net/npm/@tailwindcss/browser@4"></script>
</head>
<body class="bg-zinc-900 min-h-screen w-full text-white p-10">
    <div class="flex justify-end">
        <a href="/logout" class="bg-red-500 px-3 py-2 rounded-md">logout</a>
    </div>
    <div class="">
        <h5 class="mb-5">Edit post.</h5>
        <form action="/update/<%= post._id %>" method="post">
            <textarea class="block p-2 w-1/3 outline-none rounded-md resize-none border-2 border-zinc-800 bg-transparent" name="content"><%= post.content %></textarea>
            <input class="w-40 bg-yellow-500 px-3 py-2 rounded-md block mt-5 text-sm" type="submit" value="Edit Post">
        </form>
    </div>
</body>
</html>