<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Register</title>
    <script src="https://cdn.jsdelivr.net/npm/@tailwindcss/browser@4"></script>
</head>
<body>
    <div class="bg-zinc-900 min-h-screen w-full text-white p-10">
        <!-- buttons to go to login and profile pages -->
        <div class="flex justify-between mb-5">
            <a href="/login" class="px-5 py-2 rounded-md bg-green-500">Login</a>
            <a href="/profile" class="px-5 py-2 rounded-md bg-violet-500">Profile</a>
        </div>
        <h3 class="text-3xl mb-5">Create Account</h3>
        <form action="/register" method="post">
            <input class="px-3 py-2 outline-none bg-transparent border-2 border-zinc-800 rounded-md" type="text" name="username" placeholder="Username">
            <input class="px-3 py-2 outline-none bg-transparent border-2 border-zinc-800 rounded-md" type="text" name="name" placeholder="Name">
            <input class="px-3 py-2 outline-none bg-transparent border-2 border-zinc-800 rounded-md" type="number" name="age" placeholder="Age">
            <input class="px-3 py-2 outline-none bg-transparent border-2 border-zinc-800 rounded-md" type="email" name="email" placeholder="Email">
            <input class="px-3 py-2 outline-none bg-transparent border-2 border-zinc-800 rounded-md" type="password" name="password" placeholder="Password">
            <input class="px-5 py-2 rounded-md bg-blue-500" type="submit" value="create account">
        </form>
    </div>
</body>
</html>